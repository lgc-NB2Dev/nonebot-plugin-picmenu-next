---
status: accepted
---

# Hide Library Plugins by Default

PicMenu Next hides a library plugin from ordinary help-menu views unless its PicMenu help data explicitly sets the plugin hidden switch to visible. Library plugins primarily support other plugins, so excluding them keeps the help homepage focused on user-facing features while preserving an explicit opt-in for library plugins that intentionally expose commands.
