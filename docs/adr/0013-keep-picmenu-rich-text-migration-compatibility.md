# Keep PicMenu Rich Text Migration Compatibility

PicMenu Next continues to accept the legacy PicMenu rich-text format for migrated help content, while Markdown remains the recommended format. When legacy rich text cannot be parsed, PicMenu Next falls back to safe plain-text layout so malformed migrated content does not make the help menu unavailable.
